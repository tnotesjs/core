---
name: '文件头注释规则'
description: '编辑 .ts 和 .vue 文件时，规范文件头注释。'
applyTo:
  - '*.ts'
  - '*.vue'
---

# 文件头注释

每个 `.ts` 文件和 `.vue` 文件必须在顶部添加块注释。第一行是模块相对路径（相对于项目根目录），空行后接可选的简短描述。

`.ts` 使用 `/* */`，`.vue` 使用 `<!-- -->`。

示例：

```typescript
/*
相对路径

- 简介1
- 简介2
*/
```
